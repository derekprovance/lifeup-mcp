# LifeUp Server Configuration
LIFEUP_HOST=10.103.2.235
LIFEUP_PORT=13276

# Optional: API token if configured in LifeUp settings
LIFEUP_API_TOKEN=

# Debug logging
DEBUG=false

# Safe Mode
# When true, disables all mutation tools (create/edit/delete operations)
# Only read-only query tools will be available
SAFE_MODE=false
