# LifeUp Server Configuration
LIFEUP_HOST=127.0.0.1
LIFEUP_PORT=13276

# Optional: API token if configured in LifeUp settings
LIFEUP_API_TOKEN=

# Debug logging
DEBUG=false

# Safe Mode
# When true, disables edit/delete mutation tools while allowing create operations
# Create tools (create_task, create_achievement, add_shop_item) remain available
# Edit/delete tools (edit_task, update_achievement, delete_achievement, edit_shop_item,
# apply_penalty, edit_skill) are blocked
SAFE_MODE=false
